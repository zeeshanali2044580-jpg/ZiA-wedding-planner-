import { useEffect, useState } from 'react';
import { money, supabase } from './lib/supabase';
const nav=['Overview','Products','Categories','Packages','Halls & Services','Orders','Bookings','Payments','Customers','Audit Log'];
export default function App(){const [session,setSession]=useState(null),[section,setSection]=useState('Overview'),[products,setProducts]=useState([]),[orders,setOrders]=useState([]),[bookings,setBookings]=useState([]),[customers,setCustomers]=useState([]),[notice,setNotice]=useState('');const flash=x=>{setNotice(x);setTimeout(()=>setNotice(''),2500)};const check=async u=>(await supabase.from('admin_users').select('id').eq('id',u.id).eq('is_active',true).maybeSingle()).data;const load=async()=>{const [p,o,b,c]=await Promise.all([supabase.from('electronics_products').select('*,electronics_categories(name)'),supabase.from('orders').select('*').order('created_at',{ascending:false}),supabase.from('event_bookings').select('*,wedding_packages(title)').order('created_at',{ascending:false}),supabase.from('profiles').select('*')]);setProducts((p.data||[]).map(x=>({...x,category:x.electronics_categories?.name||''})));setOrders(o.data||[]);setBookings(b.data||[]);setCustomers(c.data||[])};useEffect(()=>{if(!supabase)return;supabase.auth.getSession().then(async({data})=>{if(data.session&&(await check(data.session.user))){setSession(data.session);load()}});const a=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>a.data.subscription.unsubscribe()},[]);const signIn=async e=>{e.preventDefault();const f=new FormData(e.currentTarget);const {data,error}=await supabase.auth.signInWithPassword({email:f.get('email'),password:f.get('password')});if(error)return flash(error.message);if(!(await check(data.user))){await supabase.auth.signOut();return flash('Admin authorization required')}setSession(data.session);load()};const update=async(table,id,patch)=>{const {error}=await supabase.from(table).update(patch).eq('id',id);if(error)flash(error.message);else{await supabase.from('audit_logs').insert({actor_id:session.user.id,action:'update',entity_type:table,entity_id:id,metadata:patch});flash('Saved');load()}};if(!session)return <div className="admin-login"><form className="login-card" onSubmit={signIn}><div className="admin-mark">Z</div><h1>ZIA Private Admin Panel</h1><input name="email" type="email" required placeholder="Admin email"/><input name="password" type="password" required minLength="8" placeholder="Password"/><button className="primary-button">Sign in securely</button><p>{notice}</p></form></div>;return <div className="admin-app-shell"><aside className="admin-sidebar"><div className="admin-brand"><div className="admin-mark">Z</div><b>ZIA Private Admin</b></div><nav className="admin-nav">{nav.map(x=><button className={section===x?'admin-link active':'admin-link'} onClick={()=>setSection(x)} key={x}>{x}</button>)}</nav><button className="admin-logout" onClick={()=>supabase.auth.signOut().then(()=>setSession(null))}>Sign out</button></aside><main className="admin-content"><header className="admin-header"><div><p>ZIA Event and Wedding Planner</p><h1>{section}</h1></div><span>● Supabase connected</span></header>{section==='Overview'&&<Overview products={products} orders={orders} bookings={bookings}/>} {section==='Products'&&<Products rows={products} update={update}/>} {section==='Orders'&&<Rows rows={orders} table="orders" update={update}/>} {section==='Bookings'&&<Rows rows={bookings} table="event_bookings" update={update}/>} {section==='Customers'&&<Table rows={customers} fields={['id','full_name','email','phone']}/>} {section==='Payments'&&<Payments/>}{section==='Audit Log'&&<Audit/>}{['Categories','Packages','Halls & Services'].includes(section)&&<Crud section={section} session={session} flash={flash}/>} {notice&&<div className="toast">{notice}</div>}</main></div>}
function Overview({products,orders,bookings}){const sales=orders.reduce((a,x)=>a+Number(x.total||0),0),advance=bookings.reduce((a,x)=>a+Number(x.advance_amount||0),0);return <div className="overview-grid"><Stat label="Products" value={products.length}/><Stat label="Orders" value={orders.length}/><Stat label="Bookings" value={bookings.length}/><Stat label="Sales / advances" value={`${money(sales)} / ${money(advance)}`}/></div>}function Stat({label,value}){return <div className="stat-card"><span>{label}</span><strong>{value}</strong><em>Live</em></div>}function Products({rows,update}){return <section className="panel-large"><h3>Prices and stock</h3>{rows.map(x=><div className="admin-row" key={x.id}><b>{x.name}</b><input type="number" min="0" value={x.price} onChange={e=>update('electronics_products',x.id,{price:Number(e.target.value)})}/><input type="number" min="0" value={x.stock_quantity} onChange={e=>update('electronics_products',x.id,{stock_quantity:Number(e.target.value)})}/></div>)}</section>}function Rows({rows,table,update}){return <section className="panel-large"><h3>{table}</h3>{rows.map(x=><div className="admin-row" key={x.id}><span>{x.id}</span><select value={x.status||'pending'} onChange={e=>update(table,x.id,{status:e.target.value})}><option>pending</option><option>confirmed</option><option>processing</option><option>completed</option><option>cancelled</option></select></div>)}</section>}function Table({rows,fields}){return <section className="panel-large"><table><tbody>{rows.map(r=><tr key={r.id}>{fields.map(f=><td key={f}>{r[f]||'—'}</td>)}</tr>)}</tbody></table></section>}function Crud({section,session,flash}){const config={Categories:['electronics_categories',['name','slug']],Packages:['wedding_packages',['title','category','price','description','is_active']],'Halls & Services':['wedding_halls',['name','city','capacity','price_from','is_active']]};const [rows,setRows]=useState([]),[form,setForm]=useState({});const [table,fields]=config[section];const load=()=>supabase.from(table).select('*').order('created_at',{ascending:false}).then(({data})=>setRows(data||[]));useEffect(load,[table]);const save=async e=>{e.preventDefault();const {error}=await supabase.from(table).upsert(form);if(error)flash(error.message);else{await supabase.from('audit_logs').insert({actor_id:session.user.id,action:'upsert',entity_type:table,entity_id:form.id||null,metadata:form});flash('Saved');setForm({});load()}};const remove=async id=>{const {error}=await supabase.from(table).delete().eq('id',id);if(error)flash(error.message);else{flash('Deleted');load()}};return <section className="panel-large"><h3>{section}</h3><form className="admin-form" onSubmit={save}>{fields.map(f=><input key={f} required={!['description','is_active'].includes(f)} placeholder={f} value={form[f]??''} onChange={e=>setForm({...form,[f]:['price','price_from','capacity'].includes(f)?Number(e.target.value):e.target.value})}/>)}<button className="primary-button">Save</button></form>{rows.map(r=><div className="admin-row" key={r.id}><span>{r.name||r.title}</span><button onClick={()=>setForm(r)}>Edit</button><button onClick={()=>remove(r.id)}>Delete</button></div>)}</section>}function Payments(){const [rows,setRows]=useState([]);useEffect(()=>{supabase.from('payment_records').select('*').order('created_at',{ascending:false}).then(({data})=>setRows(data||[]))},[]);return <section className="panel-large"><h3>Payment records</h3>{rows.map(x=><div className="admin-row" key={x.id}><span>{x.reference||x.id} · {money(x.amount)}</span><b>{x.payment_status}</b></div>)}</section>}function Audit(){const [rows,setRows]=useState([]);useEffect(()=>{supabase.from('audit_logs').select('*').order('created_at',{ascending:false}).limit(100).then(({data})=>setRows(data||[]))},[]);return <section className="panel-large"><h3>Audit log</h3>{rows.map(x=><div className="admin-row" key={x.id}><span>{x.action} · {x.entity_type}</span><small>{x.created_at}</small></div>)}</section>}
